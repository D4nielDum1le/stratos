ls = []
for i in range(3):
    try:
        print("Grapse enan akeraio ")
        number = int(input())
        ls += [number]
        ath = sum(ls)
        mo = sum(ls)/3
    except Exception:
    print("wraios akeraios re kretine")
print(ath)
print(mo)
